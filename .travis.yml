language: python
python:
  - 2.6
  - 2.7

before_install:
  - sudo apt-get update
  - sudo apt-get build-dep python-scipy python-numpy python-scipy
# command to install dependencies
install: 
 - pip install -r requirements.txt
# command to run tests
#script: nosetests
